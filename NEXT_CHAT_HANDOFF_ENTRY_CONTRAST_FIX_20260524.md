# 引継ぎ 2026-05-24 入口ページ文字色コントラスト修正

## 対象
- 全 `*Mystery/web/site.css`

## ユーザー要望
- 入口ページの文字色が背景色と近く、まだ読みづらい。

## バックアップ
- `C:\Users\kogit\Documents\Codex\backups\entry-contrast-fix-20260524`
- 全40件の `web/site.css` を退避済み。

## 変更点
- 全40件の `site.css` に `ENTRY_CONTRAST_FIX_20260524` を追加。
- 入口ページの `case-entry` 配下で以下を上書き。
  - カード背景をより濃い `rgba(4, 8, 14, .86)` に変更。
  - 本文色を `#edf4ff` へ変更。
  - 見出し色を `#ffffff` へ変更。
  - キッカーとアクセント文字を `#ffd36f` へ変更。
  - ボタン、資料リンク、メタ情報の背景と枠線を強化。

## 検証
- 全40件の入口ページをブラウザでスマホ幅390px確認。
  - 横スクロールなし。
  - `h1` 検出あり。
  - 本文色とカード背景のコントラスト確認済み。
  - `HoshigauraRailMystery`: 本文コントラスト 18.15。
  - `CrimsonClinicMystery`: 本文コントラスト 18.15。
- 全入口ページの画像参照切れ確認済み。
- `git diff --check` エラーなし。
  - LF/CRLF警告のみ。

## 次にやること
- 入口ページの読みやすさはかなり改善済み。
- 見た目をさらに整えるなら、次は各謎解きの入口背景画像の明度を個別に調整する。
