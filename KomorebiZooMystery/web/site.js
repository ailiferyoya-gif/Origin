document.addEventListener("DOMContentLoaded",()=>{const form=document.querySelector("#answerForm");if(!form)return;form.addEventListener("submit",e=>{e.preventDefault();const filled=[...form.querySelectorAll("textarea")].filter(t=>t.value.trim().length>0).length;const result=document.querySelector("#result");result.hidden=false;result.textContent=filled>=20?"20問すべてにメモがあります。公式、検索、掲示板の時系列をもう一度並べると、次の調査段階へ進めます。":filled+" / 20 問にメモがあります。空欄を埋め、同じ語が別ページでどう変化しているか確認してください。";});});